name: My first  CI pipeline 
on:
  push:
    branch:[main]
jobs:
  hello:
    runs-on:ubuntu-latest
    steps:
    -name: Checkout code
     -uses: actions/checkout@v4
      
      - name: Say hello
        run: echo "Hello from GitHub Actions, Pramit!"
      
      - name: List files
        run: ls -la